# The subagent-view bundle layer: applied when a profile lists this package
# as a bundle (dsh plugin add ...). Rows insert into the composed
# configuration; later layers may override them by id.
- insert:
    - id: subagent-view
      name: 'subagent-view'
